---
name: tcp_fixer

description: "Does the thing to fix the stuff"

templates:
  tcp_fixer_ctl.erb: bin/tcp_fixer_ctl

packages:
  - common

properties:
  nf_conntrack_tcp_timeout_time_wait:
    description: "I don't know what this does but it supposedly fixes the stuff"
    default: 10
